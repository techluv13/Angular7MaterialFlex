import { ViewReferenceDirective } from './view-reference.directive';

describe('ViewReferenceDirective', () => {
  it('should create an instance', () => {
    const directive = new ViewReferenceDirective();
    expect(directive).toBeTruthy();
  });
});
